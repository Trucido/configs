# .cwmrc 1.2.5
# Time-stamp: <2015-01-19 15:37:31 PST xoddf2>

# Appearance ###################################################################

fontname "Fixed:pixelsize=15:bold"

# Window decorations
color activeborder goldenrod1
color inactiveborder grey40

color groupborder green
color ungroupborder blue

# Urgent windows
color urgencyborder red

# Menu
color menubg black
color menufg green
color font green

# Applications #################################################################

command term urxvtc
command lock "xscreensaver-command -lock"

command tmux "urxvtc -sl 0 -e tmux attach-session"
command emacs "emacsclient -a '' -c"
command www firefox
command music audacious
command gimp gimp
command xpdf xpdf

# Keybindings ##################################################################
bind 4-t htile
bind 4-T vtile

bind 4-Escape lock

bind 4-bracketleft "mixer vol -1"
bind 4-bracketright "mixer vol +1"

bind 4-z "audacious --rew"
bind 4-x "audacious --play"
bind 4-c "audacious --pause"
bind 4-v "audacious --stop"
bind 4-b "audacious --fwd"

# To avoid conflicting with Emacs undo keybinding:
bind C-slash unmap
bind CM-slash menusearch

# Other ########################################################################

snapdist 8

# Ignored Windows ##############################################################

ignore xconsole
ignore xclock

# Groups #######################################################################

autogroup 1 URxvt
autogroup 1 Emacs
autogroup 2 Firefox
autogroup 3 Audacious
autogroup 4 feh
autogroup 4 mpv
autogroup 5 gimp
autogroup 6 Mednafen
autogroup 6 qemu-system-i386
autogroup 6 qemu-system-x86_64
autogroup 6 qemu-system-arm
autogroup 6 Xephyr
