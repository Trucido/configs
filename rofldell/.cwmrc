# .cwmrc 1.3.4
# Time-stamp: <2016-01-06 19:42:09 PST xoddf2>

# Appearance ###################################################################

fontname "Fixed:pixelsize=15:bold"

# Window decorations
color activeborder "#5FAF5F"
color inactiveborder "#808080"

color groupborder green
color ungroupborder blue

# Urgent windows
color urgencyborder "#FF5F00"

# Menu
color menubg "#1C1C1C"
color menufg "#5FAF5F"
color font "#5FAF5F"

# Applications #################################################################

command term urxvtcd
command lock "xscreensaver-command -lock"

command tmux "urxvtcd -sl 0 -name tmux -e tmux attach-session"
command emacs "emacsclient -a '' -c"
command www seamonkey
command music audacious
command gimp gimp
command xpdf xpdf
# command nes fceux
# command snes higan
# command md run-dgen
# command dmg gvbam

# Keybindings ##################################################################

# Tiling
bind 4-t htile
bind 4-T vtile

# This is easier to press than CM-delete
bind 4-Escape lock

# Cycle through current group
bind 4-Tab cycleingroup
bind 4S-Tab rcycleingroup

# Kill a window
bind CM-Escape xkill

# Volume
bind 4-bracketleft "mixer vol -1"
bind 4-bracketright "mixer vol +1"

# Audacious
bind 4-z "audacious --rew"
bind 4-x "audacious --play"
bind 4-c "audacious --pause"
bind 4-v "audacious --stop"
bind 4-b "audacious --fwd"

# To avoid conflicting with Emacs keybindings
bind C-slash unmap
bind CM-slash menusearch

bind CM-s unmap

# Other ########################################################################

snapdist 8

# Ignored Windows ##############################################################

ignore xconsole
ignore xclock
ignore xload
ignore xbiff

# Groups #######################################################################

# Main
autogroup 1 URxvt
autogroup 1 Emacs

# WWW
autogroup 2 Seamonkey

# Media
autogroup 3 feh
autogroup 3 mpv

# The GIMP
autogroup 4 gimp

# Emulators, VMs, etc
autogroup 5 Fceux
autogroup 5 Phoenix
autogroup 5 Gvbam
autogroup 5 Dgen
autogroup 5 Mednafen
autogroup 5 UAE
autogroup 5 qemu-system-i386
autogroup 5 qemu-system-x86_64
autogroup 5 qemu-system-arm
autogroup 5 VirtualBox
autogroup 5 Xephyr
