-- .xmobarrc 2.2   -*- Haskell -*-
-- Time-stamp: <2017-02-19 15:40:01 PST xoddf2>

Config { font         = "xft:Dina-8"
       , bgColor      = "#242424"
       , fgColor      = "#E5E5E5"
       , position     = BottomW R 90
       , lowerOnStart = True
       , commands     = [ Run Date "%a %b %d %H:%M:%S %Z %Y" "date" 1
                        , Run Com "conky" ["-q", "-i", "1"] "conky" 50
                        , Run StdinReader
                        ]
       , sepChar      = "%"
       , alignSep     = "}{"
       , template     = "%StdinReader% }{ %conky% <fc=#7FE0DC>%date%</fc>"
       }
